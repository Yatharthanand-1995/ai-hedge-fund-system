# AI Hedge Fund System - Environment Configuration Template
# IMPORTANT: Copy this file to .env and add your actual API keys
# Never commit .env with real keys to git!

# LLM Provider Selection (options: gemini, openai, anthropic)
LLM_PROVIDER=gemini

# Google Gemini API Key (Get free key: https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: Other LLM providers (uncomment if you want to switch)
# OPENAI_API_KEY=your_openai_key_here
# ANTHROPIC_API_KEY=your_anthropic_key_here

# Optional: News API (for enhanced sentiment)
# NEWS_API_KEY=your_news_api_key_here

# Feature Flags
ENABLE_ADAPTIVE_WEIGHTS=false

# Cache Configuration (Optional)
# CACHE_MAX_SIZE=2000        # Maximum number of analysis results to cache (default: 2000)
# CACHE_TTL_SECONDS=1200     # Time-to-live for cache entries in seconds (default: 1200 = 20 minutes)

# Monitoring & Error Tracking (Optional)
# Get your Sentry DSN from https://sentry.io/
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
# ENVIRONMENT=development  # Options: development, staging, production

# CORS Configuration (Optional)
# Comma-separated list of allowed origins, or '*' for development only
# ALLOWED_ORIGINS=http://localhost:5174,http://localhost:3000
