# Core dependencies for AI Hedge Fund System
# Data processing and analysis
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Financial data and technical analysis
yfinance>=0.2.18
talib-binary>=0.4.24  # TA-Lib for technical indicators

# Web framework for API
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# HTTP requests and async support
requests>=2.31.0
aiohttp>=3.8.0
httpx>=0.24.0

# Plotting and visualization (optional)
matplotlib>=3.7.0
plotly>=5.15.0

# Logging and utilities
python-dateutil>=2.8.0
python-dotenv>=1.0.0

# Testing (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development tools (optional)
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Additional scientific computing
scikit-learn>=1.3.0

# Time zone handling
pytz>=2023.3

# Task scheduling for automated trading
apscheduler>=3.10.0

# For caching and serialization
cachetools>=5.3.0  # Thread-safe caching with TTL
pickle-mixin>=1.0.2

# Jupyter support (optional)
jupyter>=1.0.0
ipykernel>=6.25.0

# Progress bars and CLI utilities
tqdm>=4.65.0
click>=8.1.0

# Memory optimization
psutil>=5.9.0

# Type hints support
typing-extensions>=4.7.0

# Threading and concurrency
concurrent-futures>=3.1.1  # For Python < 3.2, usually built-in

# Configuration management
pyyaml>=6.0

# AI/LLM providers for sentiment analysis (optional - gracefully degrades if not available)
openai>=1.0.0  # GPT-based sentiment analysis
anthropic>=0.8.0  # Claude API alternative for sentiment
google-generativeai>=0.8.0  # Google Gemini API (default LLM provider)

# Web scraping for news sentiment
beautifulsoup4>=4.12.0  # Web scraping for news
lxml>=4.9.0  # XML parsing for feeds

# Error tracking and monitoring
sentry-sdk[fastapi]>=2.0.0  # Error tracking and monitoring

# Note: For additional features (Redis caching, Prometheus metrics, ML models),
# see requirements-optional.txt

# AWS SDK for cloud deployment
boto3>=1.34.0  # AWS SDK for Secrets Manager and other services